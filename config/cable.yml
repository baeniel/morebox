
development:
  adapter: async
  # adapter: redis

test:
  adapter: test

production:
  adapter: redis
  # url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
   url: <%= ENV.fetch("redis://127.0.0.1") %>/<%= ENV.fetch("6379") %>
  channel_prefix: morebox_production
